# WORKLOG 2026-03-27 — aiSpeechMulti 開發日誌

## 工作摘要

今日主題：評估多引擎架構可行性，並執行 Whisper vs Google STT 離線批次比對實驗，驗證「錯誤互補性」假說。

---

## 一、多引擎架構評估

### 背景
參考 Bigme 電子閱讀器語音辨識採用多引擎切換（Microsoft、Baidu、iFLYTEK），評估本系統是否適合採用類似架構。

### 結論
- 目前系統已具備雙引擎架構（Scribe RT + Google STT chirp_3），骨架完整
- **建議優先加入 Microsoft Azure STT**（支援繁體中文 zh-TW 原生輸出、有即時串流 SDK、無大陸連線問題）
- Baidu / iFLYTEK 暫不建議（台灣連線延遲、繁體轉換負擔）

### Azure Speech Services 確認結果
| 功能 | 狀況 |
|------|------|
| 即時串流 STT | ✅ SpeechRecognizer（SDK 封裝 WebSocket）|
| 繁體中文 zh-TW | ✅ 原生輸出，不需 OpenCC 轉換 |
| 自訂詞彙（Phrase List）| ✅ 支援，與現有詞彙表方案相容 |
| Python SDK | `azure-cognitiveservices-speech`（pip 安裝）|
| 免費額度 | 每月 5 小時即時辨識 |
| 亞洲區域 | eastasia、japaneast、koreacentral 等 |

---

## 二、N-best + 詞彙表投票評估

### 評估結論
- Google STT V2 支援 `max_alternatives`，但目前程式碼只取 `alternatives[0]`
- 前提條件：`alert_keywords.json` 目前只有 21 個關鍵字，涵蓋率不足
- **建議先擴充詞彙表（100+ 術語）再啟用 N-best 投票**

---

## 三、Whisper vs Google STT 批次比對實驗

### 目的
驗證「Whisper 與 Google STT 屬於錯誤互補型」假說，評估 ensemble 效益。

### 環境準備
- 安裝 ffmpeg（conda-forge）：`/Users/apple/miniforge3/bin/conda install -y -n aiSpeech_project_nightly ffmpeg -c conda-forge`
- Whisper 20231117 已安裝，PyTorch 2.9.1 + MPS（Apple Silicon）可用

### 新增腳本
**`scripts/whisper_vs_google_batch.py`**
- 解析「彙整」文字檔作為 Ground Truth（格式：`【HH:MM:SS】filename.wav\n文字`）
- 對每個 wav 分別跑 Whisper large-v3（MPS 加速）與 Google STT chirp_3
- 計算逐檔 CER / WER，輸出比對 CSV 與摘要報告
- Resume 機制：中途中斷後重跑自動跳過已完成檔案

```bash
# 使用方式
python scripts/whisper_vs_google_batch.py                  # 完整比對
python scripts/whisper_vs_google_batch.py --skip-google    # 只跑 Whisper
python scripts/whisper_vs_google_batch.py --skip-whisper   # 只跑 Google
python scripts/whisper_vs_google_batch.py --dataset experiments/61003  # 指定資料集
```

### 實驗結果（61001 資料集，51 筆有效比對）

| 指標 | Whisper large-v3 | Google STT chirp_3 |
|------|------------------|--------------------|
| 平均 CER | 85.7% | 67.3% |
| 平均準確率 | 14.3% | **32.7%** |
| CER < 30%（良好）| 0 筆（0%）| 3 筆（5.9%）|

**互補分析**

| 類型 | 筆數 | 比例 |
|------|------|------|
| 兩者皆對（CER < 30%）| 0 | 0% |
| 僅 Whisper 對 | 0 | 0% |
| 僅 Google 對 | 3 | 5.9% |
| 兩者皆錯 | 48 | 94.1% |
| 理想 Ensemble 上限 | — | 5.9% |

### Whisper 特有問題
部分短音檔或雜訊音檔出現「幻覺」現象，輸出與音訊完全無關的內容：
- `請不吝點讚、訂閱、分享、打賞 ...`（YouTube 字幕幻覺）
- `中文字幕志愿者 李宗盛`
- `König glorifies his造型`（英文幻覺）

---

## 四、結論與下一步

### 互補假說驗證結果
**❌ 假說不成立**。兩引擎在 94.1% 樣本上同時失敗，Ensemble 效益幾乎為零。

### 根本原因診斷
1. **音訊難度**：無線電雜訊、短促對話、多人重疊，超出兩個引擎容忍範圍
2. **術語陌生**：`google_phrases.json` 缺失，兩個引擎都沒有術語導引
3. **Whisper 幻覺**：對短/噪音音訊有明顯幻覺問題，不適合本場景

### 建議優先順序（取代 Ensemble 方案）
1. **修復詞彙表**：補齊 `google_phrases.json`，讓 Google STT 吃到術語 prompt（成本最低，效益最直接）
2. **音訊前處理**：針對無線電雜訊做降噪，再送 STT
3. **強化 Google STT 單引擎**：Google 已比 Whisper 好一倍，繼續優化這條路更有效

---

## 輸出檔案

| 路徑 | 說明 |
|------|------|
| `scripts/whisper_vs_google_batch.py` | 批次比對腳本（新增）|
| `experiments/61001/asr_output/whisper/` | Whisper 逐檔辨識結果（69 個 .txt）|
| `experiments/61001/asr_output/google/` | Google STT 逐檔辨識結果（52 個 .txt）|
| `experiments/61001/evaluation/comparison.csv` | 逐檔 CER/WER 比對表 |
| `experiments/61001/evaluation/summary.txt` | 整體統計摘要 |
