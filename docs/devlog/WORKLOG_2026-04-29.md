# 工作日誌 2026-04-29

## 主題：STT 六引擎黃金語料集 baseline 評測（63 段）

延續 04-08 黃金語料集基礎建設，今日完成第一次完整 baseline 評測，
比對 6 個 STT 引擎 × 4 種後處理組合（共 24 組）的 CER 表現。

---

## 1️⃣ 黃金語料集擴充

| 項目 | 數量 |
|---|---|
| GT 段數 | **63 段**（從 6 段擴充） |
| 總時長 | 2732.7 秒（45.5 分鐘） |
| GT 總字數 | 8,223 字 |
| 事件分布 | daily 29 / control 22 / track 6 / door 4 / emergency 1 / train 1 |

Commit: `3fb063a`（57 段新增 + 6 段微修）

---

## 2️⃣ 新建批次 STT 評測工具

新增 `scripts/batch_stt_eval.py`：
- 對 `manifest.csv` 全量音檔跑指定引擎
- 結果寫入 `experiments/golden_dataset/stt_outputs/{engine_label}/{id}.txt`
- 已存在輸出預設 skip（resumable），`--force` 強制重跑
- 紀錄字數、推論時間、RTF

支援引擎：
- `chirp3` → Google STT chirp_3
- `gemini` → gemini-2.5-flash
- `gemini25pro` → gemini-2.5-pro
- `gemini31pro` → gemini-3.1-pro-preview
- `scribe` → ElevenLabs Scribe
- `sensevoice` → 本地 SenseVoiceSmall

修正項目：
- `.env` 中 `GOOGLE_APPLICATION_CREDENTIALS` 指向錯誤路徑（aiSpeech 而非 aiSpeechMulti）→ 腳本啟動時覆寫
- `GEMINI_API_KEY` env 為無效 .rtf 路徑 → 改從 `utils/api_keys.json` 讀取
- `ScribeModel` 應為 `ScribeSTTModel`

---

## 3️⃣ 6 引擎 STT 跑分結果

| 引擎 | success / 63 | 字數 | 推論時間 | RTF |
|---|---|---|---|---|
| **scribe** | 63/63 | 8,389 | 169s | 0.062 ⭐ 最快 |
| **sensevoice** | 63/63 | 6,072 | 210s | 0.079 |
| **chirp3** | 63/63 | 3,717 ⚠️ | 347s | 0.127 |
| **gemini-2.5-flash** | 62/63 | 14,299 ⚠️ | 3,297s | 1.211 |
| **gemini-2.5-pro** | 55/63 | 7,555 | 957s | 0.385 |
| **gemini-3.1-pro-preview** | 62/63 | 14,437 ⚠️ | 13,370s | 4.911 ⚠️ 最慢 |

**異常訊號**：
- `chirp3` 字數僅 GT 的 45% → 大量刪除錯誤（窄頻無線電不適合）
- `gemini-2.5-flash` 三段重複輸出（038/041/043，單段 2,619-2,743 字）
- `gemini-3.1-pro-preview` 一段重複輸出（039.txt 5,630 字）
- 所有 gemini 系列在 060 段都失敗（疑似 safety filter）

---

## 4️⃣ 24 組 CER 評測結果

| 排名 | 引擎 | baseline | +car_norm | +dict | +full(LLM) | 總改善 |
|---|---|---|---|---|---|---|
| 🥇 | **gemini-2.5-pro** | **49.34%** | 48.78% | 48.77% | 48.77% | -0.57% |
| 🥈 | **scribe** | 66.15% | 66.15% | 63.64% | 63.23% | -2.92% |
| 🥉 | **sensevoice** | 65.84% | 65.84% | 65.59% | 65.14% | -0.70% |
| 4 | gemini-3.1-pro-preview | 69.48% | 68.51% | 68.52% | 68.52% | -0.97% |
| 5 | chirp3 | 82.81% | 82.53% | 82.53% | 81.93% | -0.88% |
| 6 | gemini-2.5-flash | 177.69% ⚠️ | 176.94% | 176.98% | 163.22% | -14.46% |

---

## 5️⃣ 重大發現

### 🏆 gemini-2.5-pro 是真正的辨識王者
- baseline CER **49.34%** 比 scribe 好 17%、比 chirp3 好 33%
- 字數 7,555 / GT 8,223 = 92% 對齊
- 8 段失敗為 safety filter，非品質問題
- 後處理改善僅 -0.57% → 原始品質已高，規則庫對它幫助有限

### ⚠️ gemini-3.1-pro-preview 反而比 2.5-pro 差
- 雖然外部測試「3 vs 2 差距大」，但語音逐字稿任務 2.5-pro 更穩定
- 1 段重複輸出爆量、RTF 4.91（比 2.5-pro 慢 13 倍）
- 結論：3.1 通用知識/推理強，但語音逐字稿不是強項

### ❌ gemini-2.5-flash 不可用
- 三重失效：重複輸出 + meta talk 洩漏 + finish_reason 異常
- 即使 LLM 後修正能救回 14.46%，仍遠落後其他引擎

### 📌 SenseVoice 本地模型表現驚艷
- baseline 65.84% 幾乎追平 scribe（66.15%）
- **零 API 成本** + RTF 0.079（極快）
- 適合做離線備援與大量批次預跑

### 🔧 後處理規則庫貢獻偏小
- 對 scribe 改善 -2.92%（最好），其他引擎 <1%
- 規則覆蓋仍窄，需從錯字殘存中擴充

---

## 6️⃣ 路線圖調整

### 主力策略
- **線上主力 → gemini-2.5-pro**（CER 49%）
- **本地備援 → SenseVoice**（零成本，斷網可用）
- **Confidence 標記 → scribe**（含字級 confidence）

### 應淘汰
- ❌ gemini-2.5-flash（重複輸出失控）
- ❌ chirp3（窄頻無線電場景不適合）
- ⚠️ gemini-3.1-pro-preview（成本高、品質反退步）

### 立即行動
1. **修 Gemini `max_output_tokens`** 從 65536 拉回 8192（避免重複輸出）
2. **Dashboard 預設引擎改為 gemini-2.5-pro**
3. **規則庫擴充**：從 gemini-2.5-pro 殘存錯字抽 100+ 條補入 blacklist / contextual

---

## 7️⃣ 產出檔案

- 批次 STT 工具：`scripts/batch_stt_eval.py`
- STT 結果（6 引擎 × 各 ~63 段）：`experiments/golden_dataset/stt_outputs/{chirp3,scribe,gemini,gemini25pro,gemini31pro,sensevoice}/*.txt`
- CER 評測報告（24 組）：`experiments/llm_correction_poc/batch_eval_*.json/.md`
- 跑分日誌：`logs/batch_stt/*.log`、`logs/batch_eval/*.log`

---

## 相關連結

- 04-08 工作日誌（後處理 Pipeline 規劃）：`WORKLOG_2026-04-08.md`
- Manifest：`experiments/golden_dataset/manifest.csv`
- 後處理主模組：`scripts/post_process.py`
- 批次評測：`experiments/llm_correction_poc/batch_eval.py`

---
---

# 📝 當日下午 + 晚間實作（接續 baseline 評測）

baseline 評測完成後，依數據驅動執行 **8 大優化**，全部 commit 並 push 到 main。

## 實作總覽

| 順序 | 主題 | Commit |
|---|---|---|
| 1 | Gemini 防失控（max_output_tokens 8192 + 重複偵測） + Dashboard 主力切 2.5-pro + blacklist v1.1.0 | `839395c` |
| 2 | TermFilter overlay 機制 v2.0.0（共用基底 + 引擎 overlay） | `d27d30d` |
| 3 | contextual_corrections 也支援 engine overlay | `5bb8a5d` |
| 4 | scribe / chirp3 補 contextual rules | `d9a8860` |
| 5 | 依引擎差異化的音訊預處理（負面結果驗證） | `bf3fd89` |
| 6 | #15 錯字回饋 MVP（人工修正介面 + 自動產 draft overlay） | `b0d9b24` |
| 7 | Gemini SDK 遷移（model_gemini 部分）+ Dashboard 整合 smart-preproc | `d8af453` `15de727` |
| 8 | CER 趨勢看板（plotly + regression alert + inline diff） | `ba62093` `0a712cd` |
| 9 | google-genai SDK 全面遷移（卸載舊套件） | `6090092` |
| 10 | regression case 抽取工具（含首份報告） | `d7e74ca` `e14dc0e` |

**總計**：15 個 commit、新增/修改 ~30 個檔案、6 篇 Obsidian devlog。

---

## 1️⃣ Gemini 防失控（`839395c`）

- `max_output_tokens` 65536 → **8192**（避免重複輸出塞滿 token）
- 新增 `_truncate_runaway_repetition()`：偵測 `unit ∈ [2,30]` 連續重複 ≥5 次的失控，從首次重複處截斷（5/5 單元測試通過）
- Dashboard 預設 gemini 子模型改為 `gemini-2.5-pro`，`gemini-2.5-flash` 標「不推薦」
- blacklist v1.1.0：依 gemini25pro 抽錯字加 4 條（行控中心、兩勾三洞、兩溝三洞、兩管兩半）→ CER 48.77% → 48.35%

## 2️⃣ TermFilter overlay 機制（`d27d30d`）

實證每引擎錯字模式不同（4 條 blacklist 對 scribe / sensevoice 完全 0 命中）。把 `vocabulary/term_filter.json` 從統一規則庫拆為「共用基底 + 引擎 overlay」：

```
vocabulary/
├── term_filter.json              # 共用基底（v2.0.0）
└── engines/
    ├── gemini25pro.json          # 4 條 blacklist
    └── scribe.json               # 5 條 blacklist
```

- 加 `engine_hint` 參數 + ENGINE_ALIASES（gemini-2.5-pro → gemini25pro 等）
- `blacklist_add` / `blacklist_remove` / `whitelist_add` / `whitelist_remove` / `protected_patterns_add`
- Dashboard 規則數顯示更新
- `extract_error_pairs.py` 加 `--write-overlay` 半自動產 `*.draft.json`

## 3️⃣ contextual overlay（`5bb8a5d`）

ContextualCorrector 也納入 overlay 架構，共用同一個 `vocabulary/engines/{engine}.json`：
- 新增 `contextual_rules_add` / `contextual_rules_remove`
- 為 sensevoice 加 6 條 contextual rules（OCC 誤辨：信息→OCC、CC息→OCC 等）
- **sensevoice CER 65.59% → 64.97%（-0.62%，比 blacklist 改善 2.5x）**

## 4️⃣ scribe / chirp3 contextual（`d9a8860`）

- chirp3：4 條（九五歲站長 → G05 四維站長等）
- scribe：5 條（四为/四给站 → 四維、近戰列車 → 進站列車）
- 至此 4 主力引擎皆有專屬 overlay

## 5️⃣ 音訊預處理（負面結果）（`bf3fd89`）

ffmpeg loudnorm 預處理結果**出乎意料**：

| 引擎 | Δ raw CER |
|---|---|
| gemini-2.5-pro | -0.44% ✅ |
| sensevoice | -0.66% ✅ |
| chirp3 | **+2.76% ❌** |
| scribe | **+1.58% ❌** |

推測 chirp3 / Scribe 商業 STT 內建 frontend AGC 處理，外部 loudnorm 反而干擾。
→ 設計「依引擎差異化」：overlay 加 `audio_preprocess.enabled` 欄位，batch_stt_eval 加 `--smart-preproc` 自動路由。

## 6️⃣ 錯字回饋 MVP（`b0d9b24`）

啟動長期資料飛輪：

1. **DB schema**：`transcriptions` 加 `corrected_transcript` / `corrected_at` / `engine_hint` 欄位
2. **Dashboard**：每段辨識結果旁加可編輯文字框 + 「💾 儲存修正」/ 「↩️ 還原原文」
3. **匯出工具** `scripts/export_correction_feedback.py`：依 engine_hint 分組，可寫 `vocabulary/engines/{engine}.feedback-draft.json`

閉環：`✏️ 修正 → DB 累積 → export 抽 draft → review 改名 .json → overlay 套用 → 下次 CER ↓`

## 7️⃣ Dashboard smart-preproc 整合（`d8af453`）

把 batch_stt_eval 的 smart-preproc 邏輯也搬到 dashboard 的 `render_running_page()`：
- 自動依 `audio_preprocess.enabled` 對上傳音檔跑 ffmpeg loudnorm
- 規則顯示元件加「🎧 音訊預處理 ON/OFF」狀態
- ENGINE_ALIASES 補 `iic/SenseVoiceSmall` 等 dashboard sub_model 命名

## 8️⃣ CER 趨勢看板（`ba62093` + `0a712cd`）

四步驟構建：
1. `scripts/build_cer_index.py`：掃 batch_eval JSON 抽指標 → CSV
2. `experiments/llm_correction_poc/cer_history.csv`：聚合（11 欄、append-only）
3. `render_cer_trend_page()`：Plotly 折線圖 + 篩選 + 4 metric cards + 歷史最佳表
4. `batch_eval.py` 跑完自動 append 一筆

進階加上：
- **Regression alert**：對每個 (engine, post_process) 比對 latest vs best，超過 +5pp 紅色警示
- **Inline diff highlight**：修正 UI 即時用 difflib 字元級對齊，紅刪/綠增 HTML

## 9️⃣ SDK 遷移收尾（`6090092`）

`google-generativeai` 已停止維護，每次跑都跳 FutureWarning。完整遷移 7 個檔案：

新增 helper：
- **`utils/gemini_client.py`**：`get_client()` + `resolve_api_key()` + re-export `genai_types`

遷移：
- `model_gemini.py`（早上已遷）
- `post_process.py`（LLM 後修正）
- `gemini_keyword_extractor.py`（關鍵字擷取）
- `result_fuser_sentence.py`（句子融合 LLM 仲裁）
- `poc_llm_correction.py` / `eval_groundtruth.py` / `gemini_test.py`

→ `pip uninstall google-generativeai` 確認卸載成功，FutureWarning 完全消除。

## 🔟 Regression case 抽取（`d7e74ca` + `e14dc0e`）

`scripts/extract_regression_cases.py`：找後處理把 STT 對的字改錯的反例。

對 4 引擎 × 63 段共 252 次後處理：
- **真誤殺對：0 個** ✅（規則庫對 raw 已對字串很安全）
- 退步段：2 個，皆為「格式 / 同義詞」差異（如 number_norm 加多餘空格）

LLM 階段掃描跑 50+ 分鐘卡住中止，工具加進度印待未來再跑。

---

## 📊 4 引擎當前最佳 CER（截至 04-29）

| 引擎 | raw CER | 最佳 final CER | 用什麼 |
|---|---|---|---|
| **gemini-2.5-pro** | 49.34% | **48.03%** | preproc + car_norm + dict |
| **scribe** | 66.15% | **63.23%** | car_norm + dict + LLM |
| **sensevoice** | 65.84% | **64.51%** | preproc + car_norm + dict（含 contextual） |
| **chirp3** | 82.81% | 82.36% | car_norm + dict |

歷史最佳：gemini-2.5-pro CER **48.03%**（已套 overlay + 預處理）。

---

## 🗂️ 規則庫現況

| 檔案 | 規則數 |
|---|---|
| `vocabulary/term_filter.json` (基底) | 23 blacklist + 19 whitelist + 4 protected patterns |
| `vocabulary/engines/gemini25pro.json` | +4 blacklist + audio_preprocess=true |
| `vocabulary/engines/scribe.json` | +5 blacklist + 5 contextual + audio_preprocess=false |
| `vocabulary/engines/sensevoice.json` | +6 contextual + audio_preprocess=true |
| `vocabulary/engines/chirp3.json` | +4 contextual + audio_preprocess=false |
| `vocabulary/contextual_corrections.json` (基底) | 14 條 |

---

## 🗒️ Obsidian devlog（6 篇）

當天於 `2nd brain/20_Programming/Projects/aiSpeechMulti/_devlog/` 新增：

1. `2026-04-29 STT 六引擎 baseline 評測.md`
2. `2026-04-29 引擎專屬規則 overlay 機制.md`
3. `2026-04-29 音訊預處理負面結果與差異化方案.md`
4. `2026-04-29 錯字回饋資料飛輪 MVP.md`
5. `2026-04-29 SDK 遷移與 smart-preproc 整合.md`
6. `2026-04-29 CER 趨勢看板.md`

---

## 🎯 給明天的 TODO

### 🟡 P1 可視化系列（半天系列）
- 修正歷程查詢頁（看哪些段最常被修、字 pattern 統計）
- 趨勢頁加事件類型分組（看 daily/track/door 各自走勢）

### 🟢 P2 規劃中未做
- **#5 逐字 confidence 標記**（Scribe word-level + UI 黃色底）2d
- **#3 initial_prompt 強化驗證**（Whisper / SenseVoice 加 event_type 後 CER 對照）0.5d
- **#17 版本管理**（DB schema 加 raw / after_dict / final 5 欄）1d

### 🔧 整理型
- 整理 `experiments/llm_correction_poc/` 累積的報告檔（壓老檔 / 移歷史目錄）
- LLM 階段 regression scan 重跑（已加進度印）

### ⚠️ 已知議題
- gemini-3.1-pro-preview RTF 4.91 太慢且品質反退步 → 暫不主推
- gemini-2.5-flash 重複輸出風險仍在 → 已標「不推薦」但保留選項
- chirp3 在窄頻無線電場景 raw CER 82% 太高 → 規則救不了，需改 STT 引擎或音訊源頭

