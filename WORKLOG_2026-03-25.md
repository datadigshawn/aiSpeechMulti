# WORKLOG 2026-03-25 — aiSpeechMulti 開發日誌

## 工作摘要

今日延續昨日開發，重點在修復 WER 計算 bug、完善辨識準確率評測流程，並首次整合 Grafana 監控面板（SQLite 直連）。

---

## 一、WER（詞錯誤率）Bug 修復

### 問題描述
「純文稿比對」模式（Tab B）的 WER 計算結果永遠顯示 **0.0%、詞數 0**。

使用者提供兩份評測報告作為驗證：
- Google STT：CER=8.2%，但 WER=0.0%（異常）
- Scribe model：CER=59.0%，但 WER=0.0%（異常）

### 根本原因
`scripts/cer_engine.py` 中的 `compare_two_texts()` 函式（純文稿比對模式使用）只呼叫了 `calculate_cer()`，**完全沒有呼叫 `calculate_wer()`**，導致回傳結果裡沒有任何 WER 欄位。

`evaluate_case()`（資料夾批次評測模式）已正確呼叫 WER，只有 Tab B 的路徑漏掉。

### 修復內容（`scripts/cer_engine.py`）
- `compare_two_texts()` 新增 `wer_info = calculate_wer(gt_text, asr_text)` 呼叫
- `per_file[0]` 補上完整 WER 欄位：`wer`、`wer_accuracy`、`n_words`、`wer_sub`、`wer_del`、`wer_ins`、`wer_tokenizer`
- `overall` 補上完整 WER 統計：`mean_wer`、`total_words`、`micro_wer`、`micro_wer_accuracy`、`wer_tokenizer` 等

### 驗證方式
`_render_eval_results()` 與 `generate_text_report()` 已使用 `.get(..., 0)` 安全存取，dashboard 顯示與文字報告不需修改，重新評測即可看到正確數值。

---

## 二、Grafana 監控面板整合（SQLite 直連）

### 架構
```
aiSpeechMulti.db (SQLite)
        ↓ Docker bind mount（掛載整個 data/ 目錄）
frser-sqlite-datasource plugin
        ↓
Grafana 12.4.1 (Docker)
        ↓
http://localhost:3000
```

### 新增檔案結構
```
grafana/
├── docker-compose.yml                    ← 一鍵啟動
├── provisioning/
│   ├── datasources/sqlite.yml            ← SQLite 自動連線設定
│   └── dashboards/dashboards.yml         ← Dashboard 自動載入設定
└── dashboards/
    └── aiSpeechMulti.json                ← 完整 Dashboard（15 個 Panel）
```

### 啟動方式
```bash
cd grafana
docker compose up -d
# 開啟 http://localhost:3000
# 帳號 admin / 密碼 aiSpeech2026
```

### Dashboard 面板清單（共 15 個）
| 區塊 | Panel | 類型 |
|---|---|---|
| 整體統計 | 總辨識筆數、今日辨識、活躍管道數、關鍵字數、最高危害等級、評測事件數 | Stat |
| 辨識量趨勢 | 每分鐘辨識量（時間序列） | Time Series |
| 引擎與管道 | 各引擎佔比、各管道辨識量、VAD/降噪組合 | Pie + Bar |
| 關鍵字危害 | 危害等級分佈、關鍵字列表（L0-L5 顏色） | Bar + Table |
| 最近辨識記錄 | 最近 50 筆（含管道/引擎/VAD/信心值） | Table |
| 評測記錄 | 評測事件列表、各引擎評測次數 | Table + Bar |

### 排除的問題（Debug 過程）

**問題 1**：掛載 `../data/aiSpeechMulti.db:ro` → 錯誤「readonly database」
- 原因：SQLite SELECT 查詢也需要寫入 WAL journal 檔，`:ro` 導致全部失敗

**問題 2**：移除 `:ro` 但仍只掛載單一 `.db` 檔 → 錯誤 `SQLITE_READONLY_DBMOVED (1544)`
- 原因：SQLite 需要在**同目錄**建立 `-wal`、`-shm` 鎖定檔；單檔掛載時容器內目錄與宿主機不同

**解決方案**：改掛載整個 `../data/` 目錄 → 全部查詢回傳 `status=ok` ✅

---

## 三、Grafana 研究調查（背景知識）

### 語音辨識平台整合 Grafana 三種主要架構
1. **SQLite 直連**（已實作）：適合開發/測試；plugin: `frser-sqlite-datasource`
2. **Prometheus + Grafana**：生產環境標準；`prometheus-fastapi-instrumentator` 兩行加入 `/metrics`
3. **LGTM Stack**（Loki+Grafana+Tempo+Mimir）：完整三柱可觀測性

### 語音辨識場景建議自定義指標
- `stt_transcription_total`（Counter，含引擎/語言 label）
- `stt_transcription_duration_seconds`（Histogram）
- `stt_confidence_score`（Histogram）
- `stt_word_error_rate`（Gauge）

---

## 修改檔案彙整

| 檔案 | 類型 | 說明 |
|---|---|---|
| `scripts/cer_engine.py` | fix | `compare_two_texts()` 補上 WER 計算 |
| `grafana/docker-compose.yml` | new | Grafana Docker Compose 設定 |
| `grafana/provisioning/datasources/sqlite.yml` | new | SQLite datasource 自動佈建 |
| `grafana/provisioning/dashboards/dashboards.yml` | new | Dashboard 自動載入設定 |
| `grafana/dashboards/aiSpeechMulti.json` | new | 完整監控 Dashboard（15 panels） |
| `WORKLOG_2026-03-25.md` | new | 本日工作日誌 |

---

## 待辦事項（Next Steps）

- [ ] 評估加入 Prometheus metrics 埋點（stt_transcription_total 等自定義指標）
- [ ] 評估 Grafana Alerting：當 L5 關鍵字出現時觸發通知
- [ ] 評估 CER/WER 趨勢寫入 Prometheus Gauge，在 Grafana 畫準確率折線圖
- [ ] 驗證 Grafana Dashboard 在持續即時辨識時的即時更新效果（30 秒刷新）
