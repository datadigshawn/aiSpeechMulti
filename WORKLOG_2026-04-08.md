# 工作日誌 2026-04-08

## 主題：辨識後階段改善方向規劃（8 個候選方案）

### 背景

昨天（2026-04-07）完成三層後處理 Pipeline（車號正規化 + dict 修正 + LLM 後修正）並透過 A/B 測試驗證改善幅度：
- chirp3_v1: baseline 57.68% → full 55.36%（-2.32%）
- gemini31pro: baseline 28.70% → +car_norm+dict 26.96%（-1.74%）

原本規劃的下一步是：
- correction_dict 升級為「上下文敏感修正」
- 數字與編號正規化擴充

本日工作：**除了上述兩項，還有哪些辨識後階段可改善環節？** 進行腦力激盪與優先度評估。

---

## 候選改善方向（8 項）

依「實作成本 × 預期效益」分為三群：

### 🟢 低成本 × 高效益（建議優先）

#### 1. 雙引擎融合修正（Ensemble Correction）

**核心想法**：對同一段音訊同時跑 chirp_3 + Gemini，句子層級 CER 比對後融合。

```
音檔 ──┬──→ chirp_3  ──→ 結果 A
       └──→ Gemini   ──→ 結果 B
                            │
                            ▼
              逐句 CER 比對 + 信心度加權
                            │
                            ▼
                       最佳融合結果
```

**既有基礎**：`scripts/result_fuser.py` 已存在

**預期改善**：
- 對 chirp3 系列（57~58% baseline）預期可降至 35~40%
- 利用 chirp_3 對「通訊短句」的準度 + Gemini 對「長段落」的完整度

**實作要點**：
- 句子層級對齊（標點 + 停頓切分）
- 每句計算 `cer(A句, B句)`：<15% 直接選、>30% LLM 仲裁
- 含站碼/車廂/術語的句子優先取 Gemini

#### 2. 逐字信心分數 + 低信心標註

**核心想法**：Google STT / Whisper 提供的 word-level confidence 在 UI 上黃色高亮。

**預期改善**：
- 不改變 CER，但**大幅降低人工複核成本**
- 低信心區段自動送 LLM 修正（降 API 成本 70%+）
- 建立資料飛輪

**實作方式**：
```python
# app_dashboard.py 結果區
for word, conf in transcript_words:
    if conf < 0.7:
        st.markdown(f"<span style='background:#fff3cd'>{word}</span>", unsafe_allow_html=True)
```

#### 3. 術語黑白名單（Whitelist + Blacklist）

**核心想法**：建立絕對替換 / 絕對保留清單。

```python
BLACKLIST = {"越台門", "歐西", "哦西", "現電力一場"}
WHITELIST_KEEP = {"OCC", "EDRH", "MCP", "G07"}
```

**預期改善**：
- 解決某些錯字反覆出現的問題
- 比 correction_dict 更強硬
- 避免 LLM 誤改術語

---

### 🟡 中成本 × 中效益

#### 4. 句子對齊 + 段落級 LLM 修正

**核心想法**：目前 LLM 一次修整段，改為逐句修正並保留上下文 window（前 3 / 後 3 句只讀）。

**預期改善**：
- 對 chirp_3 系列可再降 3~5%
- 長 transcript 更穩定（避免 LLM 改寫不相關段落）
- 耗時約 × 3 但每次 prompt 更小

#### 5. CER-aware 後處理（Measured Correction）

**核心想法**：每一層修正後用輕量 LM 計算 perplexity，若上升就 rollback。

```
原文 → dict 修正 → PPL 計算
                    │
          ┌─────────┴─────────┐
          │                   │
       PPL 下降             PPL 上升
          │                   │
        套用                rollback
```

**工具**：`transformers` + `ckiplab/bert-base-chinese`

**預期改善**：
- 防止過度修正（昨天 Gemini + LLM 退步的根因）
- 自動決策每層是否套用

#### 6. 辨識結果版本管理 + A/B 回滾

**核心想法**：DB 保留每一層修正版本，UI 讓使用者選最好的。

```sql
ALTER TABLE transcriptions ADD COLUMN raw_transcript TEXT;
ALTER TABLE transcriptions ADD COLUMN after_car_norm TEXT;
ALTER TABLE transcriptions ADD COLUMN after_dict TEXT;
ALTER TABLE transcriptions ADD COLUMN after_llm TEXT;
ALTER TABLE transcriptions ADD COLUMN user_preference TEXT;
```

**預期改善**：
- 不改善 CER 但建立人工黃金資料
- 累積 100+ 樣本後可 fine-tune prompt 或訓練小模型

---

### 🔴 高成本 × 高潛力（長期）

#### 7. 領域專屬 n-gram LM 加權

**核心想法**：用歷史資料訓練捷運無線電專用 4-gram LM 重排序。

**資料來源**：
- `data/audio_archive/` 歷史修正版本
- `experiments/Test_TMRT2正確文稿/` GT

**工具**：`kenlm` + `arpa` 格式

**預期改善**：
- 比通用 LLM 更懂捷運語境
- 推論毫秒級
- CER 再降 2~5%

**風險**：需累積 50+ 對 GT 才有意義

#### 8. Whisper + Prompt Chaining 局部重跑

**核心想法**：chirp_3 低信心段落擷取音訊後用 Whisper + initial_prompt 重跑。

```
chirp_3 辨識 → 低信心段落 → 擷取音訊 WAV
                                → Whisper + 定制 prompt 重跑
                                → 與原段落融合
```

**預期改善**：
- 針對漏聽/誤聽段落補強
- 利用 Whisper 對非標準國語的耐受度
- 不全面重跑，成本可控

---

## 4 週實作路線圖

| 週次 | 任務 | 預期 CER 改善 | 工時 |
|---|---|---|---|
| **Week 1** | #1 雙引擎融合修正 | -10~15% | 2 天 |
| **Week 1** | #3 術語黑白名單 | -1~3% | 0.5 天 |
| **Week 2** | #2 逐字信心分數標註 + 低信心 LLM 修正 | -2~4% + 降 API | 2 天 |
| **Week 2** | correction_dict 上下文敏感修正 | -2~5% | 1.5 天 |
| **Week 3** | #4 句子對齊 + 段落級 LLM 修正 | -3~5% | 2 天 |
| **Week 3** | 數字與編號正規化擴充（car_number_normalizer v2） | -1~2% | 1 天 |
| **Week 4** | #5 CER-aware 後處理 | 防退步 | 2 天 |
| **Week 4** | #6 版本管理 + A/B 回滾 | 基礎建設 | 2 天 |
| **Week 5+** | #7 n-gram LM / #8 Whisper 局部重跑 | -2~5% | 長期 |

---

## 最推薦的 3 個「明天就能做」

1. **#1 雙引擎融合**：最大改善空間，且已有 `result_fuser.py` 基礎
2. **#3 術語黑白名單**：半天完成，立即見效
3. **#2 逐字信心標註**：改善 UX + 建資料飛輪

---

## 待決策

- [ ] 下一步先做哪一項？候選：
  - #1 雙引擎融合 PoC
  - #3 黑白名單 + 上下文敏感修正（組合拳）
  - #2 信心標註
  - 原規劃兩項（上下文敏感 + 數字編號正規化）

---

## 相關連結

- 昨日日誌：`WORKLOG_2026-04-07.md`
- A/B 測試腳本：`experiments/llm_correction_poc/ab_test.py`
- 後處理 Pipeline：`scripts/post_process.py`
- 既有融合邏輯：`scripts/result_fuser.py`
- PR #1（昨日成果）：https://github.com/datadigshawn/aiSpeechMulti/pull/1
