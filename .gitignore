# 環境變數（含 API 金鑰，絕對不能上傳）
.env
.env.local
.env.*.local

# Google Cloud / API 金鑰（絕對不能上傳）
utils/api_keys.json
utils/api_keys.py
utils/google-speech-key.json
utils/google-gemini-key.json
utils/google-gemini-key.rtf
*-key.json
*_key.json
*-credentials.json
*_credentials.json
service_account*.json

# Claude Code
.claude/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# 虛擬環境
venv/
.venv/
env/

# 資料庫（本地資料）
data/*.db
data/*.db-shm
data/*.db-wal

# 音訊檔案（體積大，不上傳 GitHub）
*.wav
*.mp3
*.m4a
*.flac
*.ogg
*.aac

# 暫存音檔與封存
experiments/temp_upload/
experiments/temp_youtube/
data/audio_archive/
data/test_recordings/

# 實驗資料（辨識結果保留，原始音訊排除）
experiments/*/source_audio/

# Log 檔
logs/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo

# ============================================================
# 敏感資料與機密音檔（絕對不能上傳公開 GitHub）
# ============================================================
# 捷運事件原始音檔（含實際通訊內容，涉及隱私與機密）
調查資料之純語音部份/
調查資料/
敏感資料/
confidential/
private_audio/

# 事件資料夾（依日期+事件類型命名）
experiments/202[0-9]*/
experiments/61001/
experiments/61003/

# PPTX 備份檔（保留 main 版本即可）
docs/aiSpeechMulti_Architecture_*.pptx
!docs/aiSpeechMulti_Architecture.pptx
docs/~$*.pptx

# 大型實驗輸出
experiments/llm_correction_poc/ab_test_*.json
experiments/llm_correction_poc/coverage_*.json
experiments/llm_correction_poc/eval_gt_*.json
experiments/llm_correction_poc/results_*.json

# STT 跑分快取（含實際辨識文字，不入 git）
experiments/golden_dataset/stt_outputs/

# Phase 5 fine-tune 訓練資料（含絕對路徑、可由 build_finetune_dataset.py 重建）
experiments/finetune_dataset/*.jsonl

# Phase 5 4-gram LM 模型（可由 build_ngram_lm.py 重建）
experiments/ngram_lm/*.pkl

# Phase 5 fine-tune 訓練 checkpoint + metrics（含 LoRA weights、可重訓）
# 只 ignore 子目錄（各個 run 的 .pt / metrics.json），根目錄的 eval_*.md 保留入 git
experiments/finetune_runs/*/

# Claude Design 原始包（local 參考用，不入倉；source of truth 在 static/css/）
RadioRecognition_UI/

# pytest 覆蓋率資料
.coverage
coverage.xml
htmlcov/
